apiVersion: v1
kind: Service
metadata:
  labels:
    app: signing-authority-qa
  name: signing-authority-qa
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  selector:
    app: signing-authority-qa